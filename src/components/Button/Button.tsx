import React, { FC } from "react";
import { ButtonStyles } from "./Button.styled";

const Button: FC<{ item: string }> = ({ item }) => {
  return <ButtonStyles>tem</ButtonStyles>;
};

export default Button;
